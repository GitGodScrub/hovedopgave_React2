import { createContext } from 'react';
//I am going to set up a "global" hook, so i can get easy acess throughout the app
// Using this guide: https://dev.to/ryanmoragas/global-state-in-react-2kcp

export const GlobalContext = createContext({});